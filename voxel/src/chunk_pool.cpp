#include <voxel/chunk_pool.h>

// ChunkPool is header-only (template-based), but we include the
// source file for build system consistency and potential future
// non-template implementations.

namespace jupiter {
namespace voxel {

// Implementation is entirely in the header for now.
// This file exists for build system and future extensibility.

} // namespace voxel
} // namespace jupiter
